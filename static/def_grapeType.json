{
    "type": "object",
    "description": "A grapetype used in winehub",
    "required": [
        "name"
    ],
    "properties": {
        "name": {
            "type": "string",
            "description": "Unique key for this grapetype"
        },
        "_isSuppressed": {
            "type": "boolean",
            "description": "If true, this grapetype is deprecated but should still appear on product. However no product can now be created or updated with this value"
        }
    },
    "allOf": [
        {
            "$ref": "./_translation.json"
        }
    ]
}