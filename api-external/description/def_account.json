{
    "type": "object",
    "description": "Account representing an user with ability to log in",
    "required": [
        "group",
        "preferredLanguage"
    ],
    "allOf": [
        { "$ref": "./part_model.json" },
        {
            "properties": {
                "group": {
                    "type": "string",
                    "description": "Group the account belongs to"
                }
            }
        },
        { "$ref": "./def_accountBodyPost.json" }
    ]
}